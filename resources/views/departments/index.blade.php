<x-app-layout>
    <x-slot name="header"><div class="flex items-center justify-between"><h2 class="text-xl font-semibold">Departments</h2><a href="{{ route('departments.create') }}" class="rounded bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Add department</a></div></x-slot>
    <div class="py-8"><div class="mx-auto max-w-7xl px-4">
        <div class="overflow-hidden rounded-lg bg-white shadow"><table class="min-w-full divide-y"><thead class="bg-gray-50"><tr><th class="px-5 py-3 text-left text-xs uppercase">Name</th><th class="px-5 py-3 text-left text-xs uppercase">Description</th><th class="px-5 py-3 text-left text-xs uppercase">Status</th><th class="px-5 py-3 text-left text-xs uppercase">Employees</th><th class="px-5 py-3"></th></tr></thead><tbody class="divide-y">@forelse($departments as $department)<tr><td class="px-5 py-4 font-medium">{{ $department->name }}</td><td class="px-5 py-4">{{ Str::limit($department->description, 60) }}</td><td class="px-5 py-4">{{ ucfirst($department->status) }}</td><td class="px-5 py-4">{{ $department->employees_count }}</td><td class="px-5 py-4 whitespace-nowrap"><a class="text-indigo-600" href="{{ route('departments.show', $department) }}">View</a> <a class="ml-3 text-indigo-600" href="{{ route('departments.edit', $department) }}">Edit</a><form class="js-delete-form ml-3 inline" method="POST" action="{{ route('departments.destroy', $department) }}">@csrf @method('DELETE')<button class="text-red-600">Delete</button></form></td></tr>@empty<tr><td colspan="5" class="px-5 py-8 text-center text-gray-500">No departments yet.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $departments->links() }}</div>
    </div></div>
</x-app-layout>
